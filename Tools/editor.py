###########################################// TESTING \\#############################################
import tkinter as tk
from tkinter import messagebox
import json
import os


# TODO: scene editor handles allowing users or the developer  to create scenes via simple GUI instead of text editors to manually find which line, make a more user friendly alternative and allow modifying how stories progress indirectly via the json file
class sceneEditor:
    def __init__(self):
        self.root = root


if __name__ == "__main__":
    root = tk.Tk()
    tool = sceneEditor(root)